name: First CI Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show files
        run: ls -l

      - name: Print Hello
        run: echo "Hello CI/CD 🚀"
